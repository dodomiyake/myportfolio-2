import { skills } from "@/data/portfolio";import { Section } from "@/components/ui/Section";
export function Skills(){return <Section id="skills" index="03" kicker="Technology orbit" title="Tools chosen for the problem."><p className="section-lede reveal">No inflated percentages—just technologies used to ship, learn, and build well.</p><div className="skill-grid">{skills.map((g,i)=><article className="skill-card reveal" key={g.group}><span className="skill-no">0{i+1}</span><h3>{g.group}</h3>{g.items.map(x=>{const [name,label]=x.split(" · ");return <div className="skill-row" key={x}><b>{name}</b><span>{label}</span></div>})}</article>)}</div></Section>}
